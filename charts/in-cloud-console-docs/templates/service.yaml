apiVersion: v1
kind: Service
metadata:
  name: {{ include "in-cloud-console-docs.fullname" . }}
  labels:
    {{- include "in-cloud-console-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "in-cloud-console-docs.selectorLabels" . | nindent 4 }}
